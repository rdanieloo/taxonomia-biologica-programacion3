# Perfil: persistencia MongoDB
# Activar: --spring.profiles.active=mongo
# Docker (docker compose --profile db): usuario admin / password

app.storage=mongo

spring.autoconfigure.exclude=

spring.data.mongodb.uri=mongodb://admin:password@localhost:27017/taxonomia_db?authSource=admin

